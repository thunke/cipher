== Cipher

An angular.js (using coffeescript) Rails app that serves randomized substitution ciphers.


== Setup Instructions

bundle install
npm install -g bower
bower install
rake db:create
rake db:migrate
rake db:seed
